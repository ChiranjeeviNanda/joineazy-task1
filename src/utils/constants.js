/**
 * Defines application-wide constants.
 */

// Theme Constants
export const LIGHT_THEME = "light";
export const DARK_THEME = "dark";

// Mock Data Imports (Kept here for single-source mock definitions)
export {
	MOCK_ASSIGNMENTS,
	MOCK_SUBMISSIONS,
	MOCK_USERS,
} from "../data/mockData";
